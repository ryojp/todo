upstream frontend {
    server frontend:3000;
}

upstream api {
    server api:4000;
}

server {
    listen 8080;

    location / {
        proxy_pass http://frontend;
    }

    location /api {
        rewrite /api/(.*) /$1 break;
        proxy_pass http://api;
    }
}
