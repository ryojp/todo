import AppBar from "@mui/material/AppBar";
import Typography from "@mui/material/Typography";

const NavBar = () => {
  return (
    <AppBar>
      <Typography vaariant="h6">Tasks</Typography>
    </AppBar>
  );
};

export default NavBar;
