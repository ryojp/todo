import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#113F67",
      light: "#466995",
      dark: "#00193c",
    },
  },
});

export default theme;
